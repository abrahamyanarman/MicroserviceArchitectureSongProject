apiVersion: v1
kind: Service
metadata:
  name: postgres-resource-service
  namespace: k8s-microservice-song-project
  labels:
    app: postgres-resource-service
spec:
  clusterIP: None
  selector:
    app: postgres-resource-service
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      name: pg-resource-sv