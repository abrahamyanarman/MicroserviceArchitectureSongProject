apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-resource-service-db
  namespace: k8s-microservice-song-project
spec:
  serviceName: "postgres-resource-service-db"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-resource-service-db
  template:
    metadata:
      labels:
        app: postgres-resource-service-db
    spec:
      containers:
        - name: resource-service-db
          image: postgres:latest
          envFrom:
            - secretRef:
                name: postgres-resource-service-db-secrets
          volumeMounts:
            - name: resource-data
              mountPath: /resource/postgresql/data
      volumes:
        - name: resource-data
          persistentVolumeClaim:
            claimName: rsdb-pvc
#  volumeClaimTemplates:
#    - metadata:
#        name: data
#      spec:
#        accessModes:
#          - ReadWriteOnce
#        storageClassName: manual
#        resources:
#          requests:
#            storage: 1Gi